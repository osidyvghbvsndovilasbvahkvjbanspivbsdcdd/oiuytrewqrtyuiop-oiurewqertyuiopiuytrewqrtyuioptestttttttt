[build]
builder = "DOCKERFILE"
dockerfilePath = "pdf-service/Dockerfile"

[deploy]
startCommand = "node pdf-service/index.js"
healthcheckPath = "/health"
healthcheckPort = 3001
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
